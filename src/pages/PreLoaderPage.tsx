import { PreLoaderVisual } from "@/components/PreLoaderVisual";

/** Design preview of the official branded splash. Stays until you leave. */
export function PreLoaderPage() {
  return <PreLoaderVisual />;
}
