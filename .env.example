# Public Vite env vars only (no secrets).
# VITE_API_BASE_URL=http://localhost:8000

# Optional. Same origin override for /api + media proxies (incoming local API).
# Defaults: /api → http://127.0.0.1:8090, /models → https://localhost:5080
# VITE_API_PROXY=http://127.0.0.1:8090
