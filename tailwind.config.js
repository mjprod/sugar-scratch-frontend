/** Tailwind v4 uses CSS-first config via `@theme` in `src/index.css`.
 * This file exists for tooling that still expects a JS config entry.
 */
export default {
  content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"],
};
